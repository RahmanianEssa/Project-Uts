<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nian.project_uts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EpsonActivity"></activity>
        <activity android:name=".CanonActivity" />
        <activity android:name=".PrintBrotherActivity" />
        <activity android:name=".PrintHpActivity" />
        <activity android:name=".DellActivity" />
        <activity android:name=".HpActivity" />
        <activity android:name=".AsusActivity" />
        <activity android:name=".AcerActivity" />
        <activity android:name=".AksesoriesActivity" />
        <activity android:name=".PrinterActivity" />
        <activity android:name=".LaptopActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>